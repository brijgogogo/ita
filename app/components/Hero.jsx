import React from 'react';
import Header from './HeroHeader.jsx';
import HeroContent from './HeroContent.jsx';

const Hero = () =>
      <section className="section container hero">
        <Header />
        <HeroContent />
      </section>

module.exports = Hero;
